# Fix 01: reliable values and ordinary corrections

Implemented September 22, 2026 from the clean audit checkout `d74a0ff8f865c9c064949a46d887d70692a9038d`. This report contains local results; the older counts in `PROGRESS.md` remain historical repository reports. No deployment, real data, provider call, lending-policy change, truth regeneration, or evaluation-baseline update was performed.

## Reproduced and repaired

- The untouched baseline returned N/A → 0, `($25,000)` → +25000, `25,00` → 2500, `12O,000` → 12000, and `1.234,56` → 1.23456. Its manual-value helper accepted 90/90 ownership, and form metadata reported unsigned without signature evidence. The UI lacked actions on decided values; its save code copied the old locator and forced validation/confidence.
- `src/lib/extract/{parse-value,acroform,values,validate,run}.ts` now share bounded parsing, preserve zero and negative signs, reject unsupported formats and impossible dates, and create gaps for unusable extracted values. Raw source text remains in the stored parse output. ISO and US month/day/year dates are supported. Existing field validity remains separate from parsing; partial ownership lists need not total 100%, but totals above 100% fail.
- `src/lib/deals/classification.ts` separates form recognition from unknown signature/date evidence. Existing staff filing confirmation supplies confirmed missing indicators.
- `src/lib/extract/{review,manual,review-error}.ts` and staff `FactReview.tsx`, `ValueEditor.tsx`, `actions.ts` add correction/reopen, typed ownership/funding rows, source-page/quote validation, explicitly non-verbatim transcriptions, and actionable errors. New immutable rows/events preserve old values, sources, actors, times and reasons. Corrections clear stale verifier metadata; reopen/reject trigger recalculation. Optimistic concurrency and stored-role permissions remain enforced.
- Seven Deal C paths were renamed with 100% byte identity to match existing manifests, ZIP entries and truth. `config/sample.ts`, identifier/intake helpers, seed scripts, fixture-key import, `.env.example`, README, SETUP and PILOT_RUNBOOK now use one explicit sample configuration and reject key drift. Reseeding preserves facts, document versions and decisions; links are reissued. Tests were added/updated in unit, integration, E2E and the simulated-review helper. Decisions 162–165 record the choices.

## Local verification

macOS, Node 25.6.1, pnpm 10.30.0, PostgreSQL 17 and Chromium. Fresh disposable PostgreSQL databases on port 55439 were used; no existing database was reset. `REAL_DATA_MODE=false`, mock provider only.

| Command                                             | Actual result                                                                                                                                                                                                   |
| --------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `pnpm test`                                         | 213/213 passed                                                                                                                                                                                                  |
| `pnpm test:integration`                             | 35/35 passed, including reseeding, actual follow-up identifier hashing and server-key mismatch rejection                                                                                                        |
| `pnpm test:e2e`                                     | 10 existing journeys passed; new journey initially failed on its display assertion                                                                                                                              |
| `pnpm test:e2e tests/e2e/value-corrections.spec.ts` | Passed after correcting currency formatting and distinguishing the save status from PDF-loading status; saves/history verified against the database, with unchanged processing steps and no horizontal overflow |
| `pnpm lint`, `pnpm typecheck`, `pnpm format:check`  | Passed                                                                                                                                                                                                          |
| `pnpm rules:check`                                  | Passed, unchanged packs                                                                                                                                                                                         |
| `pnpm fixtures:check`                               | Passed, all five oracles and 94 files                                                                                                                                                                           |
| `pnpm eval`                                         | All 15 gates passed; 371/371 statuses; zero false-satisfied blockers; baseline unchanged                                                                                                                        |
| `pnpm build`                                        | Passed                                                                                                                                                                                                          |
| `pnpm fixtures:generate --check`                    | Failed on Deal A's protected formation PDF: 3,645 generated bytes versus 3,634 committed bytes, with downstream ZIP/truth-hash differences                                                                      |

The same regeneration failure, including identical differing hashes, reproduced from a temporary untouched baseline checkout and with UTC. No expected outcomes or fixture contents were changed to conceal it. Exact tracked-path casing is tested even on a case-insensitive filesystem; a Linux runtime was not available. Signature images still require staff review; unsupported numeric/date formats remain unresolved. Full browser runs emitted development-server stream-close messages during navigation, but the existing browser scenarios passed. Live-provider quality was not tested. Stop after pass 1.
