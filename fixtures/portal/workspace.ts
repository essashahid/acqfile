import { person } from "../plans/shared";
/** Presentation metadata only; the accepted Deal A/C plans and bytes stay unchanged. */
export const portalWorkspace = {
  firm: "Zelmivar Advisory",
  contact: person(20260990),
  email: "adviser@example.com",
  sendBy: "2026-09-29",
};
